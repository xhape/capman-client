<div class="form-group">
  <label>{{title}}</label>
  <select id="{{id}}" class="form-control" multiple="multiple" data-placeholder="{{title}}"
          style="width: 100%;">
    {{#each options as |option|}}
      <option>{{option.name}}</option>
    {{/each}}
  </select>
</div>
