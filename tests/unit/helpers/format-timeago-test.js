import { formatTimeago } from 'capman-client/helpers/format-timeago';
import { module, test } from 'qunit';

module('Unit | Helper | format timeago');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatTimeago([42]);
  assert.ok(result);
});
